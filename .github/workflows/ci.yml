name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"
  MSRV: "1.91.0"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: Test (${{ matrix.os }}, ${{ matrix.rust }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust: [stable]
        include:
          - os: ubuntu-latest
            rust: "1.91.0"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.os }}-${{ matrix.rust }}
      - name: Run tests
        run: cargo test --workspace

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run integration tests
        run: cargo test -p torvyn-integration-tests

  wasm-e2e:
    name: Wasm End-to-End
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install wasm32-wasip2 target
        run: rustup target add wasm32-wasip2
      - uses: Swatinem/rust-cache@v2
        with:
          key: wasm-e2e
      - uses: taiki-e/cache-cargo-install-action@v2
        with:
          tool: cargo-component@0.21.1
      - name: Run real-Wasm end-to-end tests
        run: cargo test -p torvyn-integration-tests --features wasm-e2e --test test_real_wasm_e2e

  wasm-polyglot-e2e:
    name: Wasm Polyglot End-to-End
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install wasm32-wasip2 target
        run: rustup target add wasm32-wasip2
      - uses: Swatinem/rust-cache@v2
        with:
          key: wasm-polyglot-e2e
      - uses: taiki-e/cache-cargo-install-action@v2
        with:
          tool: cargo-component@0.21.1
      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"
      - name: Install TinyGo
        run: |
          # Pin to the same TinyGo minor series the project was developed
          # against. 0.41+ bundles WASI Preview-2 WIT files matching the
          # version range `wasmtime-wasi` 42 implements; older 0.3x
          # series ship a different WASI bundle that desynchronises
          # the wrapper world's `wasi:*@0.2.0` import resolution from
          # the host's linker registration.
          TINYGO_VERSION=0.41.0
          curl -sSL -o /tmp/tinygo.deb \
            https://github.com/tinygo-org/tinygo/releases/download/v${TINYGO_VERSION}/tinygo_${TINYGO_VERSION}_amd64.deb
          sudo dpkg -i /tmp/tinygo.deb
          tinygo version
      - name: Install wit-bindgen-go
        run: |
          go install go.bytecodealliance.org/cmd/wit-bindgen-go@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Run polyglot end-to-end tests
        run: cargo test -p torvyn-integration-tests --features wasm-polyglot --test test_polyglot_e2e

  doc:
    name: Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build docs
        run: cargo doc --workspace --no-deps
        env:
          RUSTDOCFLAGS: "-D warnings"

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: Generate coverage
        run: cargo llvm-cov --workspace --lcov --output-path lcov.info
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  msrv:
    name: MSRV Check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.MSRV }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: msrv
      - name: Check MSRV
        run: cargo check --workspace

  benchmark:
    name: Benchmark
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run benchmarks
        run: cargo bench -p torvyn-benchmarks
      - name: Upload criterion reports
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-reports
          path: target/criterion/
          retention-days: 30
